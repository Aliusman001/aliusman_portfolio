/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['127.0.0.1', 'localhost'], // Add any other domains you may use for images
      },
};

export default nextConfig;
